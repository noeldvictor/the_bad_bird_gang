{
  "name": "the-bad-birds",
  "private": true,
  "version": "0.1.0",
  "description": "The Bad Birds: Birds of New Jersey — M1 gray-box prototype",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "puppeteer-core": "^25.1.0",
    "typescript": "~5.6.3",
    "vite": "^6.0.0"
  }
}
